Included in the solution items folder is a the dbscript.sql file which
will create the database, tables, and procedures for all examples.
If you haven't already run that script that needs to be run.


Run that script in SQL Server Management Studio.

Run the script for
